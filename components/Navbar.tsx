import Image from "next/image"
import Link from "next/link"

const Navbar = () => {
  return (
    <nav className = "border-2 border-purple-100 flexBetween max-container padding-container relative z-30 py-5">
    <Link href="/">
      /**   <Image src="/langdorm-logo.png" alt = "logo" width = {500} height = {500}/> */
    </Link>

    
    </nav>
  )
}

export default Navbar
